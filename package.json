{
  "name": "node-vgmstream",
  "version": "0.0.1",
  "types": "lib.d.ts",
  "main": "lib.js",
  "scripts": {
    "install": "cmake-js compile --generator=Ninja --prefer-clang --out build",
    "configure:debug": "cmake-js configure --generator=Ninja --prefer-clang --out build --debug",
    "reconfigure:debug": "cmake-js reconfigure --generator=Ninja --prefer-clang --out build --debug",
    "build:debug": "cmake-js build --debug",
    "test": "node test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^8.1.0"
  }
}
